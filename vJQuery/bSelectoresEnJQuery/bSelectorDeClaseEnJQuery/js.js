$(document).ready(function() { //El simbolo dolar hace referencia a JQuery, el argumento hace referencia a lo que se esta seleccionando y lo que sigue es un método.
    //Selector de Clase.
    var p_claseColor = $('.color'); //Aca se esta seleccionando los elementos pertenecientes a la clase=color.
    p_claseColor.css("border", "3px dashed black"); //Se estan modificando las propiedades de los elementos con clase=color.
});
