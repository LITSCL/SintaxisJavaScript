$(document).ready(function() { //El simbolo dolar hace referencia a JQuery, el argumento hace referencia a lo que se esta seleccionando y lo que sigue es un método.

    $(document).tooltip(); //El método "tooltip" permite colocar un mensaje en el elemento seleccionado cuando se pasa el cursor por encima (El mensaje es el valor del atributo "title").

});
